spring:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        default_batch_fetch_size: 1000
        format_sql: true
    open-in-view: false

logging:
  level:
    org.hibernate.SQL: debug
  file:
    name: ${user.home}/logs/zoopito.log